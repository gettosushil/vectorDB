# SQLite

Local file database. Vectors are stored as BLOBs; similarity is computed in NumPy.

- Notebooks: `notebooks/`
- DB file: `data/vector-db.db` (gitignored)
- Install: `pip install -r requirements.txt`
