# Chroma

Local Chroma experiments. Point `persist_directory` at `data/` so indexes stay in this folder.

- Notebooks: `notebooks/`
- Persist dir: `data/`
- Install: `pip install -r requirements.txt`
